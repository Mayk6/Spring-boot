server:
  port: 8480

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: reader-service
aspect:
  timer:
    microseconds: false
    enabled: true